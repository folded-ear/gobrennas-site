// A soft navigation keeps a slot's last page when nothing in it matches, so
// this clears a screen left behind by navigating somewhere it doesn't cover.
export default function NoScreen() {
  return null;
}
